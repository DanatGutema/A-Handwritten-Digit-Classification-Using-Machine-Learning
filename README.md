# A-Handwritten-Digit-Classification-Using-Machine-Learning
This project builds a simple neural network using TensorFlow and trains it on the MNIST dataset to classify handwritten digits (0–9).

Loads and preprocesses the MNIST dataset

Builds a simple neural network

Trains and evaluates the model

Makes predictions on test images
